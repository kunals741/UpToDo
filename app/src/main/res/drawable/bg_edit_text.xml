<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/edit_text_background" /> <!-- Background color (white in this example) -->
    <corners android:radius="4dp" /> <!-- Corner radius of 4dp -->
    <stroke
        android:width="@dimen/dim_1dp"
        android:color="@color/edit_text_stroke_color" /> <!-- Border color (gray in this example) -->
</shape>